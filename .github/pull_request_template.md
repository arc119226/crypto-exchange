<!--
Thanks for sending this. CONTRIBUTING.md has the details; this template is
just the three things that are hard to reconstruct after the fact.

Commits need a DCO sign-off (`git commit -s`) or the dco job fails.
-->

## What was wrong

<!-- The problem, not the change. A reader in six months needs this more than the diff. -->

## What this does about it

## What you ran

<!-- `make check` covers what CI's `checks` job does minus the Docker suites. -->

- [ ] `make check`
- [ ] `make test-integration` and/or `make e2e` (needed if this touches the ledger, the chain path or the engine)
- [ ] Neither, and here is why:

## Anything you decided not to do

<!-- Optional, and often the most useful section. Scope you deliberately left alone, an approach you rejected and why. -->
