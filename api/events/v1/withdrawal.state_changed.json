{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/arc119226/crypto-exchange/api/events/v1/withdrawal.state_changed.json",
  "title": "withdrawal.state_changed",
  "description": "A withdrawal moved from one state to another (docs/plan-v1.0.md \u00a76.4.2). One event covers every transition, with previous_status and status naming the move, because the state machine is still growing and a type per state would multiply the contract by its length.",
  "type": "object",
  "required": [
    "withdrawal_id",
    "account_id",
    "asset",
    "amount",
    "to_address",
    "chain_id",
    "status"
  ],
  "properties": {
    "withdrawal_id": {
      "type": "string",
      "description": "Stable id of the withdrawal across every event about it."
    },
    "account_id": {
      "type": "string"
    },
    "asset": {
      "type": "string"
    },
    "amount": {
      "type": "string",
      "pattern": "^-?\\d{1,18}(\\.\\d{1,18})?$",
      "description": "Decimal amount in the asset's own units, never wei."
    },
    "to_address": {
      "type": "string",
      "pattern": "^0x[0-9a-f]{40}$",
      "description": "The destination the user asked for, lower-case."
    },
    "chain_id": {
      "type": "integer",
      "description": "EVM chain the withdrawal will be sent on."
    },
    "status": {
      "type": "string",
      "description": "The withdrawal's status after this event. An open string: the state machine grows with the signer, and a new state must not break a consumer."
    },
    "previous_status": {
      "type": "string",
      "description": "What the withdrawal moved away from. Absent on withdrawal.requested, which has no previous state."
    },
    "reason": {
      "type": "string",
      "description": "Why it went where it did: a policy reason, a review note, or a failure reason. Absent when there is nothing to explain."
    },
    "tx_hash": {
      "type": "string",
      "pattern": "^0x[0-9a-f]{64}$",
      "description": "The transaction now representing this withdrawal, once one exists. While a cancellation is in flight this is the displacing transaction, because that is the one whose fate decides where the money goes."
    }
  }
}
